# react02

A React starter app, modernized from Create React App to a Vite + React 19 stack.

The application lives in [`app_1/`](./app_1) — see its [README](./app_1/README.md) for the stack, scripts, and setup.

```bash
cd app_1
npm install
npm run dev
```
